version: '3.8'

services:
  db:
    image: postgres:15
    container_name: db-agendador
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: agendador_db
    ports:
      - "5433:5432"
    restart: always